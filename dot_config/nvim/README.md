# Neovim Config

My personal cross-platform Neovim configuration.

## Structure

- `lua/config/` — core Neovim settings
- `lua/plugins/` — plugin specifications
- `lazy-lock.json` — plugin version lockfile

Managed with `lazy.nvim`.
